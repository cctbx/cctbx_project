IOTA v1.1.030 - GUI updates

Bugfixes:


Features:
- Further sped-up recovery: list of image objects with all info is saved in
  analysis.pickle; only one file read in without searching for image objects,
  this seems to speed up the process, especially for large datasets (backwards
  compatibility, when analysis.pickle is lacking, is included)

Known issues:
