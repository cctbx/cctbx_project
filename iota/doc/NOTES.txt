IOTA v1.0.016 - Further GUI Updates

Features:

1. Updated monitor mode: files updated via a separate thread to make the
process less jerky when lots of images are generated rapidly
2. Restart button can pick up where aborted while run window is up

Bugfixes:

1. Suppressed annoying numpy "mean of empty slice" runtime warning
