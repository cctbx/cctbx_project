IOTA v1.1.003 - IOTA-DIALS bugfixes

1. Fixed bug when running the DIALS stills processor; the 'refine' function
returns two variables, not one.
2. Fixed crash in XTerm mode when DIALS failed to process images
3. Fixed bug in XTerm mode where run summary was not printed to screen
