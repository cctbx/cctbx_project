# The trial ladder: remaining test cases

## CURRENT QUEUE - authoritative as of 2026-09-14 (release 1.0 of the libtbx package; carried from package r43-r48)

Everything below this section is history, kept for its diagnoses; where the two disagree, this section wins.

**Done and published:** find_helices_strands; remove_poor_fragments (Swati); dock_in_map (q2lin); map_sharpening; remove_anisotropy silent skip; set_mean_sd. **Done, integrated locally, publication batch in progress (2026-09-14):** the mis-named half-map test (file 6 now runs `half_map_sharpen`) and the mis-named external-sharpening test (file 7 now runs `external_sharpen`).

**Stopped, replan agreed:** the `check_results` change (phenix_regression, `2026-09-14-check-results`) built a tail-matched comparison that mis-paired lines and did not fix the two Mac tests; it stopped at the agreed trigger with all eleven transitions explained. The replan is its own change and starts from `phenix_regression/.claude/records/2026-09-14-check-results-replan.agreed.md` (the developer's four answers: pair lines only by the diff's one-to-one alignment and never by first word; anchor the compared window by content; regenerate the three out-of-date expectations separately; one unit in the last printed digit passes). The captured comparisons (419) and their replay instrument are in that change's evidence folder and measure any candidate in minutes. **Until it lands: `test_remove_poor_fragments` and its cif twin stay red on Macs and must not be relied on to catch wrong values.**

**Open code changes, in order:**
1. **Regression harvester collects scratch copies** (phenix_regression, `command_line/test_all_parallel.py`, BOTH loops) - in progress 2026-09-14. Exclude every dot-directory path component.
2. **Roster parser moves upstream** into `cctbx_project/libtbx/test_utils/` beside `parallel.py` - the step-3 small project; source bytes are the installed `.claude/tools/` copies; the two behaviour findings (`not_all_finished` unanchored; duplicate streaming lines collapsing) travel as recorded limits. The reversal exercise happens here.
3. **F1: the sharpening tests check the wrong map** (cctbx_project, `iotbx/regression/tst_map_model_manager_model_sharpening_5.py:75-84`, shared by files 5, 6, 7): `map_model_cc()` reads `map_manager`, but sharpening writes `map_manager_scaled` (`map_model_manager.py:4427-4430, 5076-5077, 5184-5185`), so the checks see the unsharpened map and CC is 0.953 every time. **Developer's agreed meaning (2026-09-14):** the SHARPENED map (`map_manager_scaled`) must have map-to-model correlation above 0.9, the same threshold applied to the right map. Also recorded: the test's "half maps" and "external map" are copies of one map, so it exercises code paths, not real independent data.
4. **`remove_anisotropy` honours `b_cart` only when `b_iso` is truthy** (cctbx_project, `iotbx/map_model_manager.py`). **Developer's agreed meaning (2026-09-14):** a supplied `b_cart` is always honoured; a missing `b_iso` is estimated from the map; `b_iso = 0` is a real value and is used as 0.
5. **Regenerate three out-of-date expectations** (phenix_regression: `replace_side_chains_2`, its cif twin, `copy_extra_cif`) with the generator `update_wizard_tests.py`, ON ANACONDA, as a non-Python change.
6. **The `check_results` replan** (above), after 5.
7. **The does-not-reproduce trial: the Levitz report** (phenix, wxGUI2 output-directory defaults on Windows). Success is a NOT REPRODUCED closeout with a diagnostic packet and no code.
8. **map_model_manager array-length error** ("bikunleto", cctbx_project) - no reproducer supplied; the plan constructs one or the change becomes item-7-shaped.
9. The never-exercised single-map path of `remove_anisotropy`; the upstream no-protein question (needs the user's log).

**Not this project's, for the developer:** `tst_chat_window_mcp.py` and `tst_mcp_server.py` are unhealthy on the loaded server (subprocess launches of `phenix.mcp` time out at 30-60 s; the chat-window test asserts and dies in Qt thread teardown with a cleanup race); their run-to-run variation is on file in `known_failure_variation.md`.

**Procedure work (the Guide's):** step 5, the move into `libtbx/guided_coding/` with the product rename and the personal profile as `CLAUDE.local.md` (decided; a committed project `CLAUDE.md` is left alone and loads alongside it); the second-developer trial (the developer himself, from scratch, after step 3). Timing of trials is reported as the three measured figures; the developer's attention is not reported.

---

## History (superseded where the section above differs)


**Document revision r01 - 2026-09-10.** Self-contained: everything needed to continue the trials without the original conversation. Each entry gives the report as received, where the code lives, and what the trial is meant to exercise. Run each with /fix (or the front-door line in CLAUDE.local.md) in the named repository, plain-first with a caged arena ONLY where a fix-quality comparison is still wanted.

## Completed

1. **find_helices_strands NameError** (phenix module) - DONE. Fixed, tested, reviewed, integrated, pushed. Both defects (the `line_count` crash and the seed file written outside resolve's temp_dir). Record: `.claude/records/2026-09-10-fhs-ca-start-file.md`.
2. **remove_poor_fragments NoneType** (phenix module, "Swati") - the trial in progress at this document's writing. Report: map_to_model on Windows crashes with `TypeError: unsupported operand type(s) for -: 'NoneType' and 'NoneType'` at `phenix/autosol/remove_poor_fragments.py:929`, in generate_histogram, `self.delta=(self.high-self.low)/max(1.e-10,self.n)` - values never set when the value list is empty. Scientific fork the developer owns: what an empty histogram MEANS for fragment scoring.

## Remaining, in recommended order

0a. **Commit guided_coding upstream (after the shakedown and one clean v2 case).** The package itself - payload plus INSTALL.md - goes into $PHENIX/modules/cctbx_project/libtbx/guided_coding/ as a tracked cctbx package, THROUGH the v2 pipeline like any change: the procedure publishes itself. Until then the release source is the Guide's delivered packages.
0. **v2 shakedown (do first).** Three parts, in order, ATTENDED. (i) Server facts, read-only then reversible: verify the one-argument ssh quoting form (echo + exit status); establish the server environment's state against the intended PHENIX version and, with the developer's approval, bring it current by the normal update/build procedure, then freeze it; then decide and record which installation the shadow links into (SHADOW.md: read-only environment / DISPOSABLE test installation built by the normal bootstrap routine - the practical default / working installation by explicit election), and establish the shadow whose import AND harvest proofs pass. (ii) The interruption drill: launch a short remote run whose launcher has a child that OUTLIVES it and a descendant that CHANGES process group; record PGID and run token; deliberately disconnect; then demonstrate the recovery protocol - establish the job's state by explicit selection before any retry, restore, or lock release - and record plainly what the group check did and did not see. (iii) One small change end to end through the complete v2 pipeline (the reconstruction check rides it). Remote verification stays attended-only until this case's record clears it. The shakedown's products: the commissioning record, the proven shadow parameters written into the profile, and any correction the run forces back into SHADOW.md. Status 2026-09-11: parts (i) and (ii) PASSED against the working installation by election (record: 2026-09-11-v2-shakedown; shadow root proven relocatable, both proofs passed, both drill cases demonstrated); remote verification REMAINS attended-only pending (a) the developer's election recorded that the shadow links into the working installation with its exposure stated - DONE 2026-09-11, the existing proofs stand; (b) the server's stale `qrefine` module updated so t96 harvests - DONE 2026-09-11 (`git` update in modules/qrefine; baseline roster `~/t96_baseline_working_2026-09-11.log`, 2425 tests, 9 unexpected failures by name); (c) a group-changing descendant exercised in the drill - DONE 2026-09-11 (record: 2026-09-11-recommission-group-drill; all four classification rows executed, the blind spot reproduced three times). All three named conditions are closed, and remote verification NONETHELESS stays attended-only by the developer's decision, for a reason the conditions did not cover: an unattended run cannot read its own result. Exactly two items remain against it - a roster parser (named tests with outcomes, from log content only; [FAIL] lines print twice per failure; the PROFILE line rides on captured stdout, not on redirected logs) and the accepted residual that three clean checks make a retry likely but never proven safe. Part (iii) is the roster parser - DONE 2026-09-11 (record: 2026-09-11-t96-roster-parser; `.claude/tools/t96_roster.py` and `t96_roster_selftest.py`, 21 exercises, 12 of 12 mutants detected, proven on the real 25 MB baseline and on PROFILE-contaminated captured stdout, three independent outside-reader attacks surfacing as reconcile() discrepancies).

0b. **Move the roster parser upstream.** Its home is `cctbx_project/libtbx/test_utils/`, beside `parallel.py`, which owns the log format it parses. Start the session IN cctbx_project; the change is the parser plus its test, renamed to the conventional `tst_` form there, placed by the three-tier rule, verified before/after, and published through the full stage-8b gate - which will be that gate's first real exercise. Prerequisite: the parser proven in use on at least one real roster comparison. Source of the bytes: the installed `.claude/tools/` copies and the case 0(iii) record. Two behaviour-change findings travel with it, recorded and unrepaired: `not_all_finished` is matched unanchored (safe direction - it can only be raised, never cleared), and two streaming result lines for one command collapse in the dict while `reconcile()` stays clean.
3. **dock_in_map AttributeError** (phenix module). Report ("q2lin", Windows GUI): `AttributeError: 'NoneType' object has no attribute 'r'` at `phenix/autosol/dock_in_map.py:1143`, using `rt.r` where `rt` is None after an ssm_match failure. Clear deterministic crash; same shape as case 2. Exercises: the now-standard flow at full speed - this should be the timing benchmark run.
4. **A non-reproducing or environment-sensitive report** (developer to choose from incoming mail). Purpose: prove the procedure STOPS with a useful diagnostic packet - reproduction attempts preserved, smallest missing fact requested, no speculative fix - rather than manufacturing a patch. The legitimate-outcomes rule in the workflow is what is under test; success is a good closeout WITHOUT a change.
5. **map_model_manager array-length error** (cctbx_project - the OTHER repository; start the session there). Report ("bikunleto"): `RuntimeError: scitbx Error: array range error` in `iotbx/map_model_manager.py`, `_get_aniso_before_and_after`, at `tuple(flex.double(prev_b_cart) - flex.double(new_b_cart))` - length mismatch between the two b_cart lists. No concrete input reproducer supplied; the plan must construct one or the trial becomes case-4-shaped. Meatiest diagnosis of the set.
6. **set_mean_sd bin-sharing bug** (phenix module). Found and demonstrated by a plain Claude session during case 2's comparison arm (credit where due). Defect: `set_mean_sd` takes bin midpoints from `self.categories[0]` but counts from the scoring category; with different auto-computed ranges the reported mean is wrong - demonstrated: two populated categories, true mean ~250, reported 11.02. Also newly reachable via a narrow multi-file path once case 2's fix lands. A real modeling bug, bigger than a guard; the plan should settle what the correct bin-sharing behavior IS (developer decision) before construction.
7. **Regression-test harvester collects scratch copies** (test machinery: `test_all_parallel.py:35` hands the whole phenix module directory to the scan with no hidden-directory exclusion; the existing exclusion list applies only to the other repository's loop; and that other loop - phenix_regression's, `command_line/test_all_parallel.py:42-48` - harvests hidden directories too, `.git` today and `.claude` now, as the 2026-09-14 install probe showed; the pre-harvest guard applies in BOTH repositories). Found during case 2 when subagent scratch copies appeared as registered tests; it also disproved a reviewer claim that phenix-module tests need a phenix_regression wrapper. Fix shape: exclude hidden directories (any path component starting with ".") from harvesting. Keep testing.md's pre-harvest guard even after this lands.

## Queued bug reports, with their detail

**DO FIRST - and read this before trusting any wizard test.**

**D-TM1: `check_results` cannot detect a wrong value in a line repeated earlier in the log** (phenix_regression, `wizards/check_results.py` and the `wizards_cif` copy). HIGH, and it was there long before this project. A stored expectation is capped at 1000 lines but is lined up against the WHOLE log, so an expected line pairs with an identical copy earlier in the log and the changed line is discarded as surplus (`check_results.py:240-242`). Four deliberately wrong values were each reported `Test ... OK` on unchanged code; one checked line occurs 24 times, and jobs 5 and 6 of that test print identical statistics.

> **RESTRICTION, in force until the comparison is repaired: `test_remove_poor_fragments` and `test_remove_poor_fragments_cif` MUST NOT be relied on to catch wrong values.** They can still show that a change altered the output; they cannot show that it did not.

Demonstrated repair (run, not argued): compare the capped expectation against the **matching tail** of the log, with a margin for log-length change. In that form every wrong value failed with its numbers named, a difference inside the existing 1e-3 tolerance still passed, and a Mac log passed against a server-made expectation. Not measured: how many of the 27 capped expectations repeat a line earlier in their log. Evidence: record `2026-09-13-float-drift-comparison`, packet rev1, identity `1de97a1e44e8248acc3f61b00efe716a2481744435b58b136ded4911a91f5958`.

**Machine-dependent floats in expected outputs.** Live damage, unrepaired: `test_remove_poor_fragments` and its cif twin passed on every machine until 2026-09-13 and now pass only where the suite runs, because the stored expectations were regenerated on the server and three statistics are printed at full precision (`remove_poor_fragments.py:554`). **Attempted 2026-09-13 and closed without integrating**: a fixed-precision print removes the drift for those lines but catches none of the wrong values above, and would create more duplicate lines for errors to hide behind. The tail-matched comparison fixes both, so this is no longer a separate change. Candidate kept as tag `codinghelper/2026-09-13-float-drift-comparison/candidate` on `577ee0bf`.

**`check_results` turns a crash into a silent pass.** `difflib.ndiff` is wrapped in a `try/except` that replaces the expectation with `[]`, so any exception makes the comparison pass with nothing compared - live in the cif copy; in the non-cif copy line 226 re-runs `ndiff` uncaught. Related: `ndiff` degrades on DISSIMILAR input, which is why long logs hang when a test is failing, and why the 1000-line cap exists (Developer's note, 2025-02-11, `tst_resno_cif`). The cap and the silent-pass fallback are two workarounds for one defect. **A hang is visible; a silent green is not.**

**THESE THREE ARE ONE CHANGE.** All live in `check_results` and its `ndiff` behaviour. The procedure is installed in `phenix_regression` (2026-09-14, record `2026-09-14-install-procedure.md` there), and the change's terms are agreed in advance in that repository's `.claude/records/2026-09-14-check-results.agreed.md`. Run it as one change in `phenix_regression`, under those terms; do not fix these piecemeal from a phenix session.

**The roster parser compares verdicts, not failure modes - FIXED, shipped in package r37 (and carried forward).** The repaired parser (change `2026-09-13-roster-failure-modes` in cctbx_project) is the `.claude/tools/t96_roster.py` this package carries. Still owed: replace the server copies on anaconda, recompare the retained 2026-09-12 anaconda logs, and qualify the affected `MODE_CHANGED=0` claims.

**Original entry, kept for the diagnosis.** `VERIFY.md` has required comparing failure MODES since the beginning and no instrument implemented it. Two tests failing on both sides with different failure text were reported as `MODE_CHANGED=0`; an outside reader found it by reading raw logs. **Consequence: every `MODE_CHANGED=0` claim this project has made is unsupported - not wrong, unsupported** - including the 2026-09-12 "every comparison category zero". Fix the parser to compare modes, classify differences before investigating, then recompare the retained anaconda logs and qualify the affected claims. Nondeterminism is NOT grounds for dismissal: a change can introduce an intermittent failure.

**Why `remove_poor_fragments` received a model with no protein.** The upstream half of the original user crash. `map_to_model` calls it only when the sequence says PROTEIN and RNA (`map_to_model.py:6374-6386`), yet the model that arrived apparently had no protein chains. Recorded 2026-09-10 as an inference from the traceback, never observed. Needs the user's `map_to_model` log: did the model reaching "Checking to see if we need to remove poor protein chains" contain zero protein residues? If it did, the defect is in the path that decided to call it at all.


Written out here so a session that has never seen the conversation can take one. Each is a real user report or a finding from a completed change.

**remove_anisotropy honours a supplied b_cart only when `b_iso` is truthy** (cctbx_project, `iotbx/map_model_manager.py`). `analyze_aniso_object.set_up_aniso_correction` uses `b_cart_to_remove` only under `if b_cart_to_remove and b_iso:` (`cctbx/maptbx/refine_sharpening.py:2806`). So a call with a good anisotropy but `b_iso` absent - or `b_iso = 0`, the natural way for an external caller to ask for no overall B left behind - does not apply it. The two branches differ, and only one is loud: with `remove_from_all_maps = True` it RAISES `TypeError: must be real number, not NoneType` in `_print_overall_u` before any map is touched (executed on both code versions, 2026-09-12); with `remove_from_all_maps = False` it SILENTLY re-fits from the coefficients being corrected and removes that instead, and if that fit fails it removes nothing while the log names six b_cart numbers. `_print_overall_u` is called with `b_iso` at seven further sites in the same file; reachability not investigated. No caller in this tree passes no `b_iso`. What `b_iso` should be when it is not supplied is a DEVELOPER DECISION and belongs in that change's plan. Found during `2026-09-12-remove-anisotropy-none`; that record has the citations and the executed evidence.

**The single-map path of remove_anisotropy has never run in this codebase** (cctbx_project). The `remove_from_all_maps = False` branch referenced an undefined name and failed every time it was called; the 2026-09-12 change repaired it, so it now executes for the first time. Making it work is not the same as having exercised it: the test shows it returns a map instead of crashing and says nothing about whether that map is scientifically correct. Open question, not a fixed item.

**The regression harvester collects scratch copies** (`test_all_parallel.py:35`). The whole module directory is handed to the scan with no hidden-directory exclusion, so any `tst*` file under `.claude/worktrees/` becomes a registered test. Observed returning 9601 stray files from two abandoned subagent copies. Fix shape: exclude any path component beginning with a dot. Keep VERIFY.md's pre-harvest guard afterwards regardless.

**A deliberately non-reproducing report** - the Developer picks one from incoming mail. Success is a good STOP with a diagnostic packet, not a patch. This exercises the legitimate-outcomes rule, which no change has yet tested.

**`tst_map_model_manager_half_map_sharpening_6.py` does not test half-map sharpening** - it re-runs the model-sharpening test. A one-line change, low priority, noted 2026-09-12.

## Believed handled or deferred

- **combine_focused_maps map labels** (Kyle): believed already addressed by the developer before the trials; verify before spending a trial on it.
- **GUI output-directory default** (Levitz, wxGUI2): behavior/design request, not a crash; deferred as a poor trial fit.

## Standing notes for whoever continues

Plain-first caged arenas are only needed when a fix-quality comparison is wanted; the comparison design and its one contaminated run are in GuidedCoding_comparison_r03.md (or later). Time each trial: wall clock, machine working, and time waiting for the developer, as the workflow measures them; the developer's attention is not reported. One open score from case 2: whether the procedure found case 6's bug unprompted - record the answer in the comparison report. A one-time reconstruction check is still owed somewhere in this series: a fresh session reconstructs one completed change from its record alone.
