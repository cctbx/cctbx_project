XFEL UI version 0.0.001 - Initial connection to DB

1. Run tab connected to DB
    a. Thread starts run sentinel
    b. Runs and tags from DB passed on to GUI elements in Run Tab

Known issues:

1. Most connection are so far dummy connections
2. Errors returned by the Manage Tabs dialog; not sure it's working right
just yet
